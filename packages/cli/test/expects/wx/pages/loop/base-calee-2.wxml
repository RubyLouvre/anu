<view>
      <block wx:if="{{state.array && state.array.length}}">
        <block wx:for="{{state.array}}" wx:for-item="item" wx:for-index="i150">
          <view>{{item.item}}</view>
        </block>
      </block>
    </view>