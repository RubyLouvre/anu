const generate = require('babel-generator').default;
const traverse = require('babel-traverse');
const queue = require('./queue');

module.exports = (file)=>{
    let {id, ast, moduleType} = file;

};