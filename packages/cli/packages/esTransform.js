module.exports = (babel)=>{
    // eslint-disable-next-line
    const t = babel.types;
    return {
        visitor: {
            // ImportDeclaration(astPath){

            // }


        }
    };

};