<view class="container">
  {{}}
  <view bindchange="dispatchEvent" data-change-fn="checkboxChange" data-class-code="c5306781795701643" data-instance-code="{{props.instanceCode}}">
    <block wx:for="{{state.items}}" wx:for-item="item">
      <label>
        <checkbox value="{{item.name}}" checked="{{item.checked}}" />{{item.value}}
      </label>
    </block>
  </view>
</view>