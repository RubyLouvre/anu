import { cloneElement, createElement, createFactory, isValidElement } from './element/index'
import { createClass, Component } from './component/index'
import render from './element/index'

export default {
    cloneElement,
    createElement,

    createFactory,
    isValidElement,

    createClass,
    Component,

    render
}